# assembly/

Step-by-step build for ONE unit. Because every unit is identical (see
`INVARIANTS.md` R2), this one guide serves the whole fleet.

Keep it boringly literal: part, tool, action, "what good looks like." Photos beat
prose. Number the steps so two people building two spiders end up with the same
spider.
